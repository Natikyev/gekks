def leaders(arr):
    max_right = float('-inf')
    res = []

    for x in reversed(arr):
        if x >= max_right:
            res.append(x)
            max_right = x

    return res[::-1]


print(leaders([16, 17, 4, 3, 5, 2]))  # [17, 5, 2]
print(leaders([1, 2, 3, 4, 5, 2]))   # [5, 2]
