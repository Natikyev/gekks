def right_rotate(arr, d):
    n = len(arr)
    d = d % n
    for _ in range(d):
        last = arr[-1]
        for i in range(n - 1, 0, -1):
            arr[i] = arr[i - 1]
        arr[0] = last
    return arr
print(right_rotate([1, 2, 3, 4, 5, 6], 2))
print(right_rotate([1, 2, 3], 4))
