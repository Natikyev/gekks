class Node:
    def __init__(self, data):
        self.data = data
        self.next = None


def count_nodes(head):
    if head is None:
        return 0

    count = 1
    current = head.next

    while current != head:
        count += 1
        current = current.next

    return count
