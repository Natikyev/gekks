class Node:
    def __init__(self, data):
        self.data = data
        self.next = None

def convert_to_circular(head):
    if head is None:
        return None

    current = head
    while current.next:
        current = current.next
    current.next = head

    return head
